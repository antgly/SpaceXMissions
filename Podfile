# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'SpaceXMissionTracker' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SpaceXMissionTracker
  pod "Apollo"
  pod "Apollo/SQLite"
  pod "Apollo/WebSocket"

  target 'SpaceXMissionTrackerTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SpaceXMissionTrackerUITests' do
    # Pods for testing
  end

end
